## 个人页面结构修改计划

### 1. 导航栏修改
- **删除视频链接**：移除 `<a href="#" class="nav-link" data-target="videos"><i class="fas fa-video"></i> 视频</a>`
- **删除图像链接**：移除 `<a href="#" class="nav-link" data-target="photos"><i class="fas fa-image"></i> 图像</a>`
- **保留其他链接**：首页、作品、工具、文章

### 2. 右侧2/3区域修改
- **取消精选作品展示**：删除首页中的"精选作品"部分（包含 `section-header` 和 `homeProjectsGrid`）
- **添加三栏布局**：在首页中添加三个新的部分
  - **我的证书**：创建 `section-certificates` 区域，显示证书列表
  - **我的视频**：创建 `section-videos` 区域，显示视频列表
  - **我的图片**：创建 `section-photos` 区域，显示图片列表

### 3. 设置栏修改
- **修改设置标签**：调整设置栏标签，确保与新的页面结构一致
- **更新媒体设置**：确保媒体设置部分能够正确处理视频和图片的上传和管理
- **添加证书管理**：添加证书管理功能，允许上传和管理证书

### 4. 数据结构修改
- **扩展数据库结构**：在 `db.json` 中添加 `certificates` 数组，用于存储证书信息
- **修改前端渲染**：更新前端代码，渲染证书、视频和图片列表
- **更新API端点**：确保后端API能够处理证书的上传和管理

### 5. 技术实现
- **修改 `public/index.html`**：更新导航栏、首页结构和设置栏
- **修改 `api/index.js`**：添加证书管理API端点
- **更新前端JavaScript**：修改页面切换逻辑，添加证书渲染功能

### 预期效果
- 导航栏只显示首页、作品、工具、文章四个链接
- 首页右侧2/3区域显示我的证书、我的视频、我的图片三栏内容
- 设置栏能够管理证书、视频和图片
- 所有修改同步更新到数据库和前端界面